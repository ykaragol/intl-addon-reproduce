import intlMissingMessage from 'dummy/utils/intl/missing-message';
import { module, test } from 'qunit';

module('Unit | Utility | intl/missing message');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = intlMissingMessage();
  assert.ok(result);
});
